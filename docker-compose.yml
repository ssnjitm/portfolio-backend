version: "3.9"

services:
  portfolio-backend:
    build:
      context: .
      dockerfile: DockerFile
    container_name: portfolio-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
    restart: unless-stopped
